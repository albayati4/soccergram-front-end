import { useNavigate } from 'react-router-dom'
const Home = () => {
  let navigate = useNavigate()

  return (
    <div>
        <h1>This is the Home Page</h1>
      <section>
      </section>
    </div>
  )
}

export default Home